import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors (Sleek Dark Theme Palette)
  static const Color primary = Color(0xFF6C5CE7);
  static const Color primaryDark = Color(0xFF5A49E0);
  static const Color primaryLight = Color(0xFFA29BFE);
  static const Color accent = Color(0xFF00CEC9);

  // Background Colors
  static const Color background = Color(0xFF0F172A); // Slate 900
  static const Color surface = Color(0xFF1E293B);    // Slate 800
  static const Color card = Color(0xFF334155);       // Slate 700
  static const Color dialogBg = Color(0xFF1E293B);

  // Text Colors
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color spam = Color(0xFFE11D48);

  // Coin & Reward Colors
  static const Color coinGold = Color(0xFFFFD700);
  static const Color coinGlow = Color(0xFFFFAB00);

  // Shimmer Effect Colors
  static const Color shimmerBase = Color(0xFF1E293B);
  static const Color shimmerHighlight = Color(0xFF334155);
}
